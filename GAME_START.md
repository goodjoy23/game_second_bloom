# Godot 게임 시작 안내

현재 구현된 범위는 3D 북카페를 직접 걸어 다니며 제1장 첫 만남을 시작하는 플레이 가능한 프로토타입이다.

## 실행 방법

1. Godot 4.x를 실행한다.
2. `가져오기` 또는 `Import`를 선택한다.
3. 이 폴더의 `project.godot` 파일을 선택한다.
4. 프로젝트가 열리면 오른쪽 위의 실행 버튼 또는 `F5`를 누른다.
5. 시작 화면에서 플레이할 캐릭터를 선택한다.
6. 방향키 `← ↑ ↓ →`로 이동한다. 캐릭터는 이동 방향으로 회전하며, 다른 인물에게 가까이 가면 말풍선 대화가 자동으로 시작된다.
7. 대답 선택지는 방향키 `↑/↓`로 이동하고 `스페이스바`로 확정한다.

벽과 창문 경계, 책장, 테이블, 카운터, 화분에는 3D 충돌체가 있다. 오브젝트를 통과하지 않고 가구 사이 통로로 이동한다.

프로젝트 위치:

```text
D:\게임\중년의연애소설\project.godot
```

## 현재 플레이 기능

- 원근감 있는 3D 북카페와 비 내리는 창문 연출
- 벽·창문·가구를 통과하지 못하게 하는 3D 충돌
- 5인 플레이 캐릭터 선택과 방향키 이동
- 이동 방향을 따라 부드럽게 회전하는 캐릭터
- 선택하지 않은 4인의 NPC 자동 배치
- 인물 근처에서 자동으로 시작하는 말풍선 대화
- 일반 NPC의 캐릭터별 자동 응답
- 남성·여성 시점에 따라 달라지는 첫 만남 대화
- 윤서정 시점의 첫 인사에서 기존 제1장으로 이어지는 흐름
- 카페 안에서 작게 움직이는 NPC
- 민우와 서정의 첫 만남 대화
- 글자 타이핑 연출과 스페이스바 진행
- 3번의 선택지 분기
- 애정·신뢰·존중·서운함 상태 변화
- 인물별 감정 표시와 대화 초점 변화
- 자동 저장, 이어하기, 처음부터 다시 보기
- 1280×720 기준 반응형 UI

## 파일 구조

```text
project.godot                 Godot 프로젝트 설정
scenes/main.tscn              3D 월드 뷰포트, 캐릭터와 대화 UI
scripts/main.gd               대화 진행 및 선택지 처리
scripts/player_controller.gd  선택 캐릭터의 3D 이동과 방향 회전
scripts/cafe_world_3d.gd      북카페 메시, 조명과 충돌 오브젝트
scripts/game_state.gd         관계 상태와 저장
data/chapter_01.json          제1장 실제 게임 대사
tests/validate_chapter.gd     대화 연결 검사
tests/validate_exploration.gd 3D 충돌, 회전과 접근 대화 검사
```

## 다음 제작 순서

1. 빗소리, 문 종, 공구 소리와 배경 음악을 연결한다.
2. 카페의 추가 조사 지점과 짧은 독백을 만든다.
3. 제1장 두 번째 에피소드 `남은 나사 하나`를 추가한다.
4. 대화 기록과 수동 저장 슬롯을 구현한다.

## 개발자용 데이터 검사

Godot 실행 파일이 명령줄에서 인식되는 환경이라면 다음 방식으로 제1장 데이터의 끊어진 연결을 검사할 수 있다.

```text
godot --headless --path . --script res://tests/validate_chapter.gd
```

이동 화면과 접근 대화 전환은 다음 명령으로 검사한다.

```text
godot --headless --path . --script res://tests/validate_exploration.gd
```
